import { FC } from 'react'

const SearchWeather: FC = () => {
    return (
        <>
            <input type="text" />
            <button>Serach</button>
        </>
    )
}

export default SearchWeather